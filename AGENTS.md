在撰写博客时，请严格遵循以下撰写规则：

1. 除 gif 图片外，其他所有图片都要转换为 webp 格式，在不损失图片质量的情况下尽量减少图片文件大小；
2. 所有图片都放到 `docs/assets/` 目录下，并修改文中引用路径为 `assets/images/图片文件名.webp`；
3. 所有图片加载方式都要使用 Markdown 图片语法 ![alt](path)
4. 文件名不能使用中文名，一律使用英文名；
5. 所有代码都放置在 `code/` 目录下，所有文档都放置在 `docs/` 目录下。
6. 文档中禁止链接到被 gitignore 忽略的本地路径（尤其是 `code/`）；未发布的代码示例只能用纯文字说明，不得出现死链。
7. 所有图片必须带有非空且描述性的 alt 文本（如 `![安装界面截图](...)`）；
8. 新增图片统一使用英文命名 `模块-主题-序号.webp`，禁止使用截图默认名或时间戳名；
9. 未被任何文档引用的孤儿图片应移入 `docs/assets/archive/` 归档，不得长期堆放在 `images/` 中。
